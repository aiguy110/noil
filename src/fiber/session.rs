use uuid::Uuid;

pub struct FiberSession {
    pub fiber_id: Uuid,
    // TODO: Define fiber session state
}

impl FiberSession {
    pub fn new() -> Self {
        todo!("implement FiberSession")
    }
}
